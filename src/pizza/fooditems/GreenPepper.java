package pizza.fooditems;

// A class representing a type of food item, specifically Green Pepper
public class GreenPepper extends FoodItem {

    // Constructor to initialize a GreenPepper object with a given name
    public GreenPepper(String name) {
        // Call the constructor of the superclass (FoodItem) with the given name
        super(name);
    }
}
