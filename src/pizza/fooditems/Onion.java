package pizza.fooditems;

// A class representing a type of food item, specifically onions
public class Onion extends FoodItem {
    
    // Constructor to initialize an Onion object with a given name
    public Onion(String name) {
        // Call the constructor of the superclass (FoodItem) with the given name
        super(name);
    }
}
