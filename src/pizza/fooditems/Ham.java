package pizza.fooditems;

// A class representing a type of food item, specifically ham
public class Ham extends FoodItem {
    
    // Constructor to initialize a Ham object with a given name
    public Ham(String name) {
        // Call the constructor of the superclass (FoodItem) with the given name
        super(name);
    }
}
