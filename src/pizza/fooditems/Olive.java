package pizza.fooditems;

// A class representing a type of food item, specifically olives
public class Olive extends FoodItem {
    
    // Constructor to initialize an Olive object with a given name
    public Olive(String name) {
        // Call the constructor of the superclass (FoodItem) with the given name
        super(name);
    }
}
